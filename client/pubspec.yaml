name: client
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  grpc: ^3.0.2
  protobuf: ^2.1.0
  flutter_riverpod: ^1.0.4
  bloc: ^8.0.3
  riverbloc: ^1.0.0+2
  universal_platform: ^1.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.1.11
  protoc_builder: ^0.2.1+1

flutter:
  uses-material-design: true